# Canonical benchmark artifacts

`harbor.mecob` is regenerated from `benchmarks/packages/harbor/root.meco` with its message manifest
by the CLI. It is a checked-in `bytecode/0` fixture for native, Deno, browser, and embedding
measurements, not a frozen release format.
